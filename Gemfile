source 'https://rubygems.org'

# note: not released yet
#gem 'rails', '4.0.0.beta'

# note: need to use git version of journey, for there's no gem right now 
gem 'journey', github: 'rails/journey'

# note: hash conditions finder is deprecated
gem 'activerecord-deprecated_finders', github: 'rails/activerecord-deprecated_finders'

# note: not released yet, so need to ues git version edge rails
# Bundle edge Rails instead:
gem 'rails', github: 'rails/rails'

gem 'mysql2'

# Gems used only for assets and not required
# in production environments by default.
group :assets do
  gem 'sprockets-rails', github: 'rails/sprockets-rails'
  # gem 'sass-rails',   '~> 4.0.0.beta'
  # gem 'coffee-rails', '~> 4.0.0.beta'
  
  # note: need to use git verion for edge rails
  gem 'sass-rails',   github: 'rails/sass-rails'  
  gem 'coffee-rails', github: 'rails/coffee-rails'

  # See https://github.com/sstephenson/execjs#readme for more supported runtimes
  # gem 'therubyracer', platforms: :ruby

  gem 'uglifier', '>= 1.0.3'
end

gem 'jquery-rails'

# To use ActiveModel has_secure_password
# gem 'bcrypt-ruby', '~> 3.0.0'

# To use Jbuilder templates for JSON
# gem 'jbuilder'

# Use unicorn as the app server
# gem 'unicorn'

# note: for ruby 1.9 webrick bug, use Thin
# bundle exec rails s thin
gem 'thin'

# Deploy with Capistrano
# gem 'capistrano', group: :development

# To use debugger
# gem 'debugger'
